{
  "name": "@polymorph/adapter-swift",
  "version": "0.0.0",
  "description": "Polymorph Swift adapter — build-time codegen from a Polymorph theme to a Swift `PolymorphTheme` enum/struct (SwiftUI Color, CGFloat, Font, TimeInterval, cubic-bezier tuples, shadows). No Xcode toolchain required to generate.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "vitest run",
    "update-goldens": "tsx scripts/update-goldens.ts"
  },
  "dependencies": {
    "@polymorph/spec": "workspace:*",
    "@polymorph/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "tsx": "4.19.2",
    "vitest": "2.1.8"
  }
}
