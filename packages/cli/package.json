{
  "name": "@polymorph/cli",
  "version": "0.0.0",
  "description": "Polymorph CLI: validate / lint / resolve (transform is post-v1).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "polymorph": "./dist/cli.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@polymorph/spec": "workspace:*",
    "@polymorph/core": "workspace:*",
    "@polymorph/loaders": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "vitest": "2.1.8"
  }
}
