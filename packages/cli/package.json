{
  "name": "@polymorph/cli",
  "version": "0.0.0",
  "description": "Polymorph CLI: validate / lint / resolve / transform (token transform wraps Style Dictionary).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "polymorph": "./dist/cli.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
