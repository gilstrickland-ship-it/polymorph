// @polymorph/core — runtime resolution core (TypeScript) for web + React Native.
//
// Implemented in Spec B: parse → validate against schema → advisory a11y lint →
// resolve aliases → select theme mode → emit a flat resolved token map for adapters.

export const CORE_VERSION = "0.0.0";
