{
  "name": "@polymorph/core",
  "version": "0.0.0",
  "description": "Polymorph resolution core: validator, advisory a11y linter, alias resolver, mode selection.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
