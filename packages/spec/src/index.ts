// @polymorph/spec — the theme contract (the standard).
//
// Implemented in Spec A: DTCG-extended token schema, the semantic vocabulary
// (the finite, versioned set of purpose-named tokens the SDK codes against),
// component-token + theme-mode conventions, and JSON Schema for validation.

export const SPEC_VERSION = "0.0.0";
